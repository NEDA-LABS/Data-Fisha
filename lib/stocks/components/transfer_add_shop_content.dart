import 'package:flutter/material.dart';
import 'package:smartstock/core/components/BodyLarge.dart';

Widget transferAddShopContent() => Container(
      padding: const EdgeInsets.all(24),
      child: const BodyLarge(text:
        "Create shop in transfer not support yet. Make sure you have more than one shop for your account",
      ),
    );
